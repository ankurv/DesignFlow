Product idea: ${idea}
Human steering: ${steering}
Current request: ${task}
Capability catalog:
${capabilities}

Draft complete initial artifacts under ## DESIGN_UPDATE, ## PLAN_UPDATE, and ## DECISIONS_UPDATE. Preserve user directives and optionality. Record material decisions with status, alternatives, trade-offs, rationale, and revisit conditions. PLAN must include Requirement Traceability mapping every explicit brief outcome to design coverage, bounded work, and acceptance evidence. Choose diagrams by explanatory need, not quota.

For every selected capability behavioral contract, add a `### capability.id` subsection under `## Capability Behavioral Contracts`. Name every required decision dimension exactly and resolve it. Include explicit `Decisions`, `Failure states`, `Implementation`, and `Acceptance` labels. Apply a documented safe default when the choice is routine; ask the user only when the choice materially changes product behavior, risk, privacy, or cost. Add the capability ID to PLAN.md Requirement Traceability.
